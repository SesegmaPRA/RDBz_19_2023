/******************************************************************************
Программа: создать 4 переменных разного типа, продемонстрировать работу четырех
арифметических операторов на выбор
*******************************************************************************/

#include <iostream>
int main()
{
    int x, y;
    x = 123;
    y = 2;
    std::cout << x*y << std::ends;
}
int main()
{
    float x, y;
    x = 1.23;
    y = 2.1;
    std::cout << x*y << std::ends;
}
int main()
{
    char x;
    x = 'g';
    std::cout << x << std::ends;
}
int main()
{
    bool x;
    x = true;
    std::cout << x << std::ends;
}
/*
1) Программа: создать 4 переменных разного типа, продемонстрировать работу четырех арифметических операторов на выбор
*/
#include <iostream>

using namespace std;

int main()
{

    int x, y;
    x = 123;
    y = 2;
  cout << x*y << ends;

    float x, y;
    x = 1.23;
    y = 2.1;
    cout << x*y << ends;

    char x;
    x = 'g';
    cout << x << ends;

    bool x;
    x = true;
    cout << x << ends;
}

    return 0;
}

